# Config

## Schema

```
{
    purge: Array{String}
    outDir: String
}
```

- `purge` HTML files to purge, example: ["\*\*/\*.html"]
- `outDir` Output directory, example: "dist"

All values relative to config file.
