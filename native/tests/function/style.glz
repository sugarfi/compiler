fn add(a, b)
	return $a + $b

fn double(a)
	return $a * 2

fn factorial(n)
	if $n < 2
		return 1
	else
		return $n * factorial($n - 1)

.class
	font-weight: (add(3, 4) - 1) * double(50)
	padding: {factorial(4)}px
